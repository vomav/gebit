<core:FragmentDefinition
   xmlns="sap.m"
   xmlns:core="sap.ui.core"
   xmlns:form="sap.ui.layout.form">
   	<Dialog
		title="{i18n>users}">
		<Input id="assignFreestyleName" 
			/>
		<endButton>
			<Button text="{i18n>assignToUser}" press="onAddUnregisteredUser"></Button>
		</endButton>
		<beginButton>
			<Button text="{i18n>cancel}" press="closeAddUregisteredUserDialog"></Button>
		</beginButton>
	</Dialog>
</core:FragmentDefinition>